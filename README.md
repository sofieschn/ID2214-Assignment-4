## Installing
```
$ pip install -r requirements.txt
```

## Adding any libraries:
Remember to run the command:
```
$ pip freeze > requirements.txt
```
When adding any new libraries

## Running
- Assumes you have training_smiles.csv alread
- Run notebook 1 in order to generate training_featires.csv (it's quite big, so can't push it straight to github)